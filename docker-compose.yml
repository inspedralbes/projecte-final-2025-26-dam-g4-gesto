services:
  backend:
    restart: always
    build: ./backend
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/usr/src/app
      - ./gesto:/usr/src/gesto
      - /usr/src/app/node_modules
    env_file:
      - ./backend/.env

  frontend:
    restart: always
    build: ./gesto
    ports:
      - "80:3000"
    volumes:
      - ./gesto:/app
      - /app/node_modules
    depends_on:
      - backend
